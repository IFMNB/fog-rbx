--!strict
const Service = require("@game/ReplicatedStorage/class/core/Service")

const class = Service:new {
    add_character = function (self, character: character, ...: user) : ()
        
    end,

    rem_character = function (self, character: character, ...: user) : ()
        
    end,

    get_character = function (self, of: user) : character?
        
    end
}

return class