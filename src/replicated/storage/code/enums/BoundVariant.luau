--!strict

const EnumItem = require("../class/arch/EnumItem")
const EnumObject = require("../class/arch/EnumObject")

const declare = require("../static/declare")

const class  = EnumObject:new({
	name = "BoundVariant",

    --[=[
        Вариант `BoundBox`, который заставляет коробку учитывать
        только размер и положение в пространстве, таким образом
        сохраняя неполный отпечаток геометрии

        Применяется в качестве хитбоксов или описания
        объемов, а вообще не ограничен природой своего существования
        и может быть использован в любых целях
    ]=]
	Alive = EnumItem:new({
		name = "AABB",
	}),

    --[=[
        Вариант `BoundBox`, который заставляет коробку учитывать
        не только размер и положение в пространстве, но и поворот
        объекта, таким образом сохраняя отпечаток геометрии

        Применяется в качестве хитбоксов или описания
        объемов, а вообще не ограничен природой своего существования
        и может быть использован в любых целях
    ]=]
    Dead = EnumItem:new({
        name = "OBB"
    })
})

return class